import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const MemoCallback = (props: Props) => {
  return (
    <View>
      <Text>MemoCallback</Text>
    </View>
  );
};

export default MemoCallback;

const styles = StyleSheet.create({});
